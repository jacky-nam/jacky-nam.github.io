{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "portfolio",
      "runtimeExecutable": "python",
      "runtimeArgs": [
        "-c",
        "import os, functools, http.server; handler = functools.partial(http.server.SimpleHTTPRequestHandler, directory='.'); port = int(os.environ.get('PORT', '5173')); http.server.ThreadingHTTPServer(('', port), handler).serve_forever()"
      ],
      "port": 5173,
      "autoPort": true
    }
  ]
}
